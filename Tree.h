#pragma once
#include "Book.h"
#include <iostream>
#include <string>

class TreeNode {
	string Name;
	int color;
	TreeNode *parent;
	TreeNode *left;
	TreeNode *right;
	void Tree - Insert() {
		color[x] = RED;
		while x! = root[T
	}
};
